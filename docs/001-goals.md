# Goals

I want to develop a solution that can work with standard ontology files (like the ones in turtle format). My goal is
to have diagramming capability for ontology files: visually render parts of an ontology on a diagram. 