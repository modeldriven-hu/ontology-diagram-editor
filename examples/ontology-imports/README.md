# OWL Import Example

Add `example.ttl` through the Ontology Diagram model tree. Its relative
`owl:imports` declaration causes `example-foundation.ttl` to be added first.

The main ontology also contains a relationship with multiple domains and ranges for
testing endpoint selection.
